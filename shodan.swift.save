//===-----------------------------------------------===//
//  The WebAPI class interfaces with the shodanhq.com/api
// Author: Alexandre Antonio Juca
//===------------------------------------------------===//

struct Shodan {
  private let baseUrl = "https://api.shodan.io/"
  public var apiKey = "" // Could be set to nil instead
  
  init (apiKey: String) {
    self.apiKey = apiKey    
    print("[DEBUG] apiKey is set to -> \(self.apiKey). ")
  }

  // Internal method that makes an HTTP Request to the shodan endpoint
  // Requires a webservice function (ex. 'search' 
  private func request( 

}

let shodan = WebAPI(apiKey: "aX212kdsd")

